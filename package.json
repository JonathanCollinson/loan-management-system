{
  "name": "loan-management-system",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev:api": "docker compose stop api 2>/dev/null; docker compose up -d mongo && pnpm --filter @lms/api run start:dev",
    "dev:api:docker": "docker compose up -d",
    "dev:web": "pnpm --filter @lms/web run dev",
    "build": "pnpm --filter @lms/api run build && pnpm --filter @lms/web run build",
    "test": "pnpm --filter @lms/api run test",
    "test:e2e": "pnpm --filter @lms/web run test:e2e",
    "lint": "pnpm --filter @lms/api run lint && pnpm --filter @lms/web run lint",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  }
}
